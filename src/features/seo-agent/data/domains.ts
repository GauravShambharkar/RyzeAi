export type Domain = string;

// Hardcoded for now — swap for the authed user's connected properties when
// the domain-picker is wired to real data.
export const availableDomains: Domain[] = ["get-ryze.ai"];
